/**
 * Navbar
 */

(function() {
    'use strict';

    // Smooth scroll
    /*
    var navbarList = $('#navbar-list')
    navbarList.onePageNav({
        currentClass: 'active',
        changeHash: false,
        scrollSpeed: 750,
        scrollThreshold: 0.5,
        filter: '',
        easing: 'swing'
    });
    */
})();
